import numpy as np
import matplotlib.pyplot as plt

#PERCEPTRON TRAINING 
#Input : array X -> N x m
#      : array T -> N (target values)

# Randomly sort the inputs to the algorithm before each epoch 
#code up the loss function and compute the loss after each epoch in testing
# linear function: f(x1,x2) = 2x1 + 3x2 -1